import { Meta } from "@storybook/addon-docs/blocks";

<Meta title="Scaffold check" />

# Scaffold check

Proves the W0 scaffold works: Storybook 10 boots, MDX renders, Tailwind v4 compiles, and the Google Fonts load.

**Lane W1 (ticket 02) replaces `tokens/dist/tokens.css` with the real token layer.** Until then the swatches below use Tailwind's stock palette, not Raqt's. Lane W2 (ticket 03) deletes this page.

<div className="flex gap-2">
  <div className="size-16 rounded-lg bg-emerald-500" />
  <div className="size-16 rounded-lg bg-emerald-700" />
  <div className="size-16 rounded-lg bg-neutral-800" />
</div>

<p style={{ fontFamily: "Archivo, sans-serif", fontStretch: "115%", fontSize: "2rem", fontWeight: 700, letterSpacing: "-0.01em" }}>
  Archivo Expanded — display
</p>

<p style={{ fontFamily: "Inter, sans-serif", fontSize: "1rem" }}>
  Inter — body text.
</p>
