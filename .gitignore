node_modules/
dist/
# ...except the emitted theme: the registry serves it and consumers read it from git.
!tokens/dist/
storybook-static/
.DS_Store
*.log
.env*
!.env.example
.wayfinder/

# design-sync: build artifacts, scratch, verification state. The durable set
# (config.json, NOTES.md, conventions.md, previews/, overrides/) stays committed.
.design-sync/sb-reference/
.design-sync/learnings/
.design-sync/.cache/
.design-sync/node_modules
.design-sync/*.log
.design-sync/upload.txt
.ds-sync/
ds-bundle/
